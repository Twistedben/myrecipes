<%= render 'shared/page_title', title: "Signup as a Chef!" %>
<!--  We change the "obj:" variable below to our isntance variable @chef, which is defined in our
chefs_controller.rb -->
<%= render 'form' %>
<!--  We don't need to change the "obj" or instance variable here because we know we'll always be dealing
with @chef in this view. -->
